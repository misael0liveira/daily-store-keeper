# DevMasterIA

Para o contexto completo deste projeto, consulte [AGENTS.md](./AGENTS.md).

Regras:
- leia AGENTS.md antes de alterar código;
- inspecione o fluxo existente antes de implementar;
- preserve o funcionamento do APK;
- valide funcionalidades reais, não apenas a renderização;
- nunca exponha secrets;
- execute as verificações apropriadas antes de concluir uma tarefa.

Os playbooks especializados ficam em `.devmasteria/`.
